import {resources} from 'library/index';

export const mainMenu = () => {
    // Testing statistics with different modes (activation always through user input, else too intrusive?)

    const resourcesTR = resources['tr'];

    console.log(resourcesTR);
};
